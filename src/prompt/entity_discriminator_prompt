你是一位知识图谱本体建模专家，正在处理一个面向“汽车造型设计”领域的知识图谱构建任务。

下面是当前抽取出来的原始实体类型列表，其中一些可能与造型设计主题无关，另一些类型之间存在语义上的重复或细粒度划分。你的任务是：

1. **删除** 与“汽车外观造型设计”无直接关系的实体类型，例如仅涉及工程结构、车辆分类、功能零件或统计数值的类型。
2. **合并** 意义相近或可以统一建模的实体类型。例如：`UserProfile` 和 `UserPersona` 可以合并为一个“用户角色”类。
3. 请**只保留被保留和合并后的类型**，不要输出已删除的类型。
4. 输出为一个 **List[List[str]]**，其中每个子列表表示可以合并的实体类型。

造型设计关注的核心要素包括但不限于：尺寸比例、视觉感知、线条姿态、空间结构、用户审美、情感表达等。

请根据专业理解输出合并结果，以下是原始实体类型列表：

{entity_type_list}
